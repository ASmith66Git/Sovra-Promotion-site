services:
  - type: web
    name: sovra-promotion-site
    env: static
    buildCommand: npm install && npm run build:static
    staticPublishPath: dist/public
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
